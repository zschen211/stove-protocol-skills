[project]
name = "stove-protocol-skills"
version = "0.1.0"
description = "Scripts to crawl Stove Protocol docs and convert them to Markdown."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "beautifulsoup4>=4.12.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
package = false

